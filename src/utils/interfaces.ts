export interface Coords {
  row: number;
  col: number;
}

export interface Cell {
  row: number;
  col: number;
  cell: number;
}

export interface LeaderBoardInterface {
  name: string;
  score: number;
}
